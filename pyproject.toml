[project]
name = "tweedledum"
requires-python = ">=3.6"
dynamic = ["version", "classifiers", "authors", "license", "readme"]

[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cmake>=3.18",
    "ninja",
    "scikit-build>=0.12.0",
]
